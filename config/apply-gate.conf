# Final real-machine apply gate for the reviewed V1 execution candidate.
# This flag enables the guarded code path only; it does NOT set REAL_MACHINE_APPROVED=true.
REAL_APPLY_FEATURE_ENABLED=true
REAL_APPLY_REQUIRE_TTY=true
REAL_APPLY_REQUIRE_CURRENT_COMMIT_DRY_RUN=true
REAL_APPLY_REQUIRE_VERIFIED_BACKUP=true
REAL_APPLY_REQUIRE_EXACT_CONFIRMATION=true
REAL_APPLY_CONFIRMATION_PHRASE=JE_CONFIRME_L_EXECUTION_REELLE_UBUNTU_DESKTOPS_CUSTOM
REAL_APPLY_PHASE_CONFIRMATION=true
REAL_APPLY_BACKUP_MAX_AGE_SECONDS=86400
REAL_APPLY_BACKUP_PROOF_FILE=state/real-apply/backup-verified.pass
REAL_APPLY_DRYRUN_PROOF_FILE=state/real-apply/full-dry-run.pass
